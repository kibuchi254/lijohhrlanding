---
/** * PREMIUM KENYAN HR & PAYROLL AI HERO 
 * Features: Perfect SVG Annotations, KES Currency, Layered Glassmorphism
 */
---

<section class="relative min-h-screen w-full flex items-center justify-center overflow-hidden bg-white px-4 md:px-12 py-16 lg:py-24 font-sans">
  
  <div class="absolute inset-0 z-0">
    <div class="absolute inset-0 opacity-[0.04]" style="background-image: radial-gradient(#030A8C 1px, transparent 1px); background-size: 30px 30px;"></div>
    <div class="absolute top-[-15%] right-[-5%] w-[50%] h-[50%] bg-gradient-to-br from-[#030A8C]/10 to-transparent rounded-full blur-[120px]"></div>
    <div class="absolute bottom-[-10%] left-[-5%] w-[40%] h-[40%] bg-gradient-to-tr from-[#F29F05]/10 to-transparent rounded-full blur-[100px]"></div>
  </div>

  <div class="max-w-7xl w-full relative z-10">
    <div class="flex flex-col lg:flex-row items-center gap-12 lg:gap-4">
      
      <div class="w-full lg:w-[58%] space-y-10 text-center lg:text-left">
        
        <div class="inline-flex items-center gap-3 bg-white border border-gray-100 p-1.5 pr-5 rounded-full shadow-sm">
          <span class="bg-[#030A8C] text-white text-[10px] font-black px-3 py-1.5 rounded-full tracking-widest uppercase">AI Engine v4.0</span>
          <span class="text-sm font-bold text-[#020659]">Kenya's #1 Precision Payroll</span>
        </div>

        <h1 class="text-5xl md:text-6xl lg:text-7xl font-black text-[#0D0D0D] leading-[1.05] tracking-tight">
          Modern 
          <span class="relative inline-flex items-center justify-center px-4 py-1">
            HR
            <svg class="absolute inset-0 w-full h-full text-[#F29F05] opacity-90 scale-125" viewBox="0 0 100 100" fill="none" preserveAspectRatio="none">
              <path d="M5,50 C5,10 95,10 95,50 C95,90 5,90 7,55" stroke="currentColor" stroke-width="4" stroke-linecap="round" class="draw-path"></path>
            </svg>
          </span> 
          & 
          <span class="relative inline-block">
            Payroll
            <svg class="absolute -bottom-2 left-0 w-full h-3 text-[#030A8C] opacity-60" viewBox="0 0 100 20" preserveAspectRatio="none">
              <path d="M2,15 Q50,5 98,15" stroke="currentColor" stroke-width="6" stroke-linecap="round" class="draw-path-slow"></path>
            </svg>
          </span>
          <br /> built for accuracy.
        </h1>

        <p class="text-gray-500 text-lg md:text-xl max-w-xl mx-auto lg:mx-0 leading-relaxed font-medium">
          Scale your Kenyan enterprise with AI. From KRA compliance to instant M-Pesa salary disbursements, we've automated the hard part.
        </p>

        <div class="flex flex-wrap items-center justify-center lg:justify-start gap-6">
          <button class="bg-[#F29F05] text-[#0D0D0D] px-10 py-5 rounded-2xl font-black text-lg shadow-[0_20px_40px_-10px_rgba(242,159,5,0.4)] hover:-translate-y-1.5 transition-all duration-300">
            Start Free Trial
          </button>
          
          <button class="flex items-center gap-3 font-black text-[#0D0D0D] group py-4">
            <div class="w-12 h-12 rounded-full border-2 border-gray-100 flex items-center justify-center group-hover:bg-[#030A8C] group-hover:text-white transition-all duration-300">
              <svg class="w-4 h-4 fill-current ml-0.5" viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
            </div>
            Watch Product Tour
          </button>
        </div>

        <div class="flex items-center justify-center lg:justify-start gap-4 pt-6">
          <div class="flex -space-x-3">
            {[1, 2, 3].map((i) => (
              <div class="w-12 h-12 rounded-full border-4 border-white overflow-hidden shadow-md bg-gray-100">
                <img src={`https://api.dicebear.com/7.x/avataaars/svg?seed=kenya${i}`} alt="User" />
              </div>
            ))}
          </div>
          <p class="text-sm font-bold text-gray-400">
            <span class="text-[#0D0D0D]">5,000+</span> Kenyan HR Teams
          </p>
        </div>
      </div>

      <div class="w-full lg:w-[42%] relative">
        <div class="relative w-full max-w-[480px] mx-auto animate-float-slow">
          
          <div class="bg-white/80 backdrop-blur-2xl rounded-[48px] border border-white shadow-[0_50px_100px_-20px_rgba(3,10,140,0.1)] p-8 md:p-10 relative z-20">
            
            <div class="flex justify-between items-center mb-10">
              <div class="space-y-1">
                <p class="text-[#030A8C] font-black text-[10px] uppercase tracking-[0.2em]">Total Payroll (KES)</p>
                <h2 class="text-4xl font-black text-[#0D0D0D] tracking-tighter">KES 4,820,400</h2>
              </div>
              <div class="w-10 h-10 rounded-2xl bg-gray-50 flex items-center justify-center border border-gray-100">
                 <div class="w-1.5 h-1.5 rounded-full bg-emerald-500 animate-pulse"></div>
              </div>
            </div>

            <div class="flex items-end justify-between h-44 gap-3 px-1">
               <div class="w-full bg-[#030A8C]/5 rounded-xl h-[40%] transition-all hover:bg-[#030A8C]/10"></div>
               <div class="w-full bg-[#030A8C]/5 rounded-xl h-[65%]"></div>
               <div class="relative w-full h-full bg-[#030A8C] rounded-xl shadow-2xl overflow-hidden group">
                  <div class="absolute inset-0 opacity-20 bg-[repeating-linear-gradient(45deg,transparent,transparent_8px,#fff_8px,#fff_16px)]"></div>
                  <div class="absolute inset-0 flex items-center justify-center">
                    <span class="[writing-mode:vertical-lr] text-[9px] font-black text-white/40 tracking-[0.2em] uppercase">Optimized</span>
                  </div>
               </div>
               <div class="w-full bg-[#030A8C]/5 rounded-xl h-[55%]"></div>
               <div class="w-full bg-[#030A8C]/5 rounded-xl h-[80%]"></div>
            </div>

            <div class="mt-8 p-4 bg-gray-50 rounded-3xl border border-gray-100 flex items-center justify-between">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-emerald-500 rounded-xl flex items-center justify-center text-white font-black text-xs">M</div>
                <div>
                  <p class="text-[10px] font-black text-gray-400 uppercase">M-Pesa Bulk</p>
                  <p class="text-xs font-bold text-[#0D0D0D]">Instant Payouts</p>
                </div>
              </div>
              <span class="text-[10px] font-black text-emerald-600 bg-emerald-50 px-2 py-1 rounded-lg tracking-tighter">CONNECTED</span>
            </div>
          </div>

          <div class="absolute -top-6 -right-4 bg-[#0D0D0D] text-white p-5 rounded-[28px] shadow-2xl border border-white/10 z-30">
             <p class="text-[9px] text-[#F29F05] font-black uppercase tracking-widest">KRA Compliance</p>
             <p class="text-sm font-bold mt-1">P9 Forms Generated</p>
          </div>

          <div class="absolute -bottom-8 -left-8 bg-white/95 backdrop-blur-xl border border-gray-100 p-6 rounded-[32px] shadow-2xl z-30 hidden md:block">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-2xl bg-[#030A8C]/5 flex items-center justify-center">
                <svg class="w-6 h-6 text-[#030A8C]" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
              </div>
              <div>
                <p class="text-[10px] font-black text-gray-400 uppercase">Precision Level</p>
                <p class="text-xl font-black text-[#0D0D0D]">99.99%</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="mt-24 pt-10 border-t border-gray-50 flex flex-wrap justify-center lg:justify-between items-center gap-10 grayscale opacity-30">
       <span class="text-xl font-black text-[#020659] tracking-tighter">SAFARICOM_PLC</span>
       <span class="text-xl font-black text-[#020659] tracking-tighter">EQUITY_GROUP</span>
       <span class="text-xl font-black text-[#020659] tracking-tighter">KCB_BANK</span>
       <span class="text-xl font-black text-[#020659] tracking-tighter">ZAMARA_KE</span>
       <span class="text-xl font-black text-[#020659] tracking-tighter">BRITAM_HQ</span>
    </div>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;700;800&display=swap');

  section {
    font-family: 'Plus Jakarta Sans', sans-serif;
  }

  /* SVG DRAWING ANIMATION */
  .draw-path {
    stroke-dasharray: 400;
    stroke-dashoffset: 400;
    animation: draw 2.5s ease-out forwards;
    animation-delay: 0.3s;
  }
  .draw-path-slow {
    stroke-dasharray: 200;
    stroke-dashoffset: 200;
    animation: draw 1.5s ease-out forwards;
    animation-delay: 1.5s;
  }

  @keyframes draw {
    to { stroke-dashoffset: 0; }
  }

  @keyframes float-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }
  .animate-float-slow {
    animation: float-slow 6s ease-in-out infinite;
  }

  h1 { text-wrap: balance; }
</style>